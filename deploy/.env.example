# =============================================
# SpecFlow Deployment Configuration
# Copy to .env and modify as needed:
#   cp .env.example .env
# =============================================

# PostgreSQL
POSTGRES_DB=specflow
POSTGRES_USER=specflow
POSTGRES_PASSWORD=changeme

# Spring Boot
SPRING_PROFILES_ACTIVE=prod
