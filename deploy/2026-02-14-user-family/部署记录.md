# SoulPal Service 部署记录

**部署日期**: 2026-02-14
**部署版本**: `633e45e` - 完成了User相关的功能（P1+P2的开发）
**部署人员**: Claude Code / 自动化部署

---

## 📦 部署内容

本次部署包含以下模块的完整实现：

### 1. User 模块（P1 + P2）
- 用户注册/登录/登出
- 用户信息管理
- 宠物信息管理
- 邮箱验证功能

### 2. Family 模块（新增）
- 家庭创建与管理
- 家庭成员管理
- 家庭邀请系统（通过邀请码）
- 家庭所有权转移

---

## ✅ 部署前检查

| 检查项 | 状态 |
|--------|------|
| Git 状态（无未提交变更） | ✅ 通过 |
| Checkstyle 代码检查 | ✅ 通过 |
| 单元测试 + 集成测试 | ✅ 通过 |
| 代码已推送至 origin/main | ✅ 已是最新 |

---

## 🚀 部署执行

### 本地推送
```bash
git push origin main
# Everything up-to-date
```

### 远程部署
```bash
ssh home-node "cd /srv/soulpal-service/deploy && bash deploy.sh"
```

部署脚本执行流程：
1. ✅ 拉取最新代码 (`git pull origin main`)
2. ✅ 构建 Docker 镜像 (Maven + Docker)
3. ✅ 启动服务容器 (postgres + redis + api)
4. ✅ 健康检查通过

---

## 🏥 部署验证

### 容器状态
```
NAME               STATUS                    PORTS
soulpal-api        Up 28 seconds             0.0.0.0:8080->8080/tcp
soulpal-postgres   Up 35 seconds (healthy)   0.0.0.0:5432->5432/tcp
soulpal-redis      Up 35 seconds (healthy)   0.0.0.0:6379->6379/tcp
```

### 健康检查结果

**内网健康检查** (`localhost:8080`):
```json
{
  "status": "UP",
  "components": {
    "db": { "status": "UP", "details": { "database": "PostgreSQL" } },
    "diskSpace": { "status": "UP" },
    "livenessState": { "status": "UP" },
    "ping": { "status": "UP" },
    "readinessState": { "status": "UP" },
    "ssl": { "status": "UP" }
  }
}
```

**外网健康检查** (`https://api.soulpal.me`):
```json
{"status":"UP", ...}
```

---

## 📊 当前版本信息

| 项目 | 值 |
|------|-----|
| 部署时间 | 2026-02-14 |
| Commit Hash | `633e45e` |
| Commit Message | 完成了User相关的功能（P1+P2的开发） |
| API 地址 | https://api.soulpal.me/swagger-ui/index.html |
| 健康检查 | https://api.soulpal.me/actuator/health |

---

## 📋 发布后验证清单

- [x] 容器状态正常: `docker compose ps` 显示 3 个容器 running/healthy
- [x] 本地健康检查通过: `{"status":"UP"}`
- [x] 公网健康检查通过: `{"status":"UP"}`
- [x] 数据库连接正常（health 响应中包含 `db: UP`）
- [x] Cloudflare Tunnel 连接正常

---

## 🔄 回滚方案（如需要）

如需回滚到上一版本 (`f7d1498`):

```bash
ssh home-node
cd /srv/soulpal-service
git checkout f7d1498
cd deploy
docker compose up -d --build
```

---

**文档生成时间**: 2026-02-14
**部署状态**: ✅ 成功
