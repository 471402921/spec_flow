# Tech Pack 模板（小团队 + AI 80%）

> 模块名称：`<模块名>`（`<批次标识，如 P0/P1/P2>`）
> 版本：`v0.1`
> 状态：`草稿 / 快审中 / 已基线`
> 更新时间：`YYYY-MM-DD`
> 负责人：`后端主责`
> 基于 PRD：`doc/requirements/<module>-prd.md vX.Y`
> 前置依赖：`<如有前置批次的 Tech Pack，在此引用>`

---

## 原则

Tech Pack 只记录**约束和决策**。字段设计、接口参数、测试代码等实现细节由 AI 在开发阶段根据架构规范（CLAUDE.md）自动生成，无需在此文档中预定义。

---

## 1. 输入基线

- PRD 版本：`vX.Y`
- 关键规则ID范围：`BR-01 ~ BR-xx`
- 验收项范围：`AC-01 ~ AC-xx`
- 前置条件：`<已完成的批次和已就绪的基础设施>`
- 功能范围：
  - `<功能A>`
  - `<功能B>`
- 非目标（本批次不实现）：
  - `<排除项A>`
  - `<排除项B>`
- 体量假设：`<用户规模、数据库策略>`

---

## 2. 规则映射总表

> 确保每条 PRD 规则都有技术落点，不丢规则。

| PRD规则ID | DB约束 | API行为 | 测试点ID | 状态 |
|----------|--------|---------|----------|------|
| BR-01 | `<唯一索引/字段约束>` | `<接口行为>` | TC-01 | 待开发 |
| BR-02 | `<约束>` | `<行为>` | TC-02 | 待开发 |

---

## 3. 数据库设计（只记录决策）

### 3.1 核心实体

| 表名 | 用途 | 备注 |
|------|------|------|
| `<table_a>` | `<用途>` | 新建/修改 |

### 3.2 关键约束与决策

> 只写需要人确认的约束，字段细节和索引由 AI 根据规则映射自动设计。

**`<table_a>` 表：**
- `<唯一性约束：如 email 全局唯一>`
- `<软删策略：deleted 字段，保留周期>`

**`<table_b>` 表：**
- `<强一致事务场景：如 xxx 必须在同一事务>`

**决策项（需人确认）：**

| # | 决策项 | 建议 | 状态 |
|---|--------|------|------|
| D-01 | `<决策问题>` | `<建议方案>` | 待确认 |
| D-02 | `<决策问题>` | `<建议方案>` | 待确认 |

---

## 4. API 设计（只记录清单与约束）

### 4.1 接口清单

| API-ID | 方法 | 路径 | 用途 | 鉴权 | 优先级 |
|--------|------|------|------|------|--------|
| API-01 | POST | `/api/v1/xxx` | `<用途>` | 是/否 | P0 |
| API-02 | GET | `/api/v1/xxx/{id}` | `<用途>` | 是/否 | P0 |

### 4.2 关键约束（逐条，只写决策项）

> 不写 request/response 字段细节，只写 AI 无法自行判断的约束。

#### API-01 `<名称>`

- 规则映射：`BR-xx, AC-xx`
- 特殊约束：`<如：频率限制、并发冲突处理、幂等策略>`

### 4.3 鉴权方案（仅首批次需要，后续批次标注"复用 P0 鉴权方案"即可）

- 认证方式：`<如 Session Token / JWT>`
- 认证流程：`<如 Interceptor 提取 token → 验证有效性 → 注入 userId>`
- 排除路径：`<不需要鉴权的路径列表>`

### 4.4 错误码规范

| 错误码 | HTTP状态 | 语义 | 用户提示 |
|--------|----------|------|----------|
| `<CODE_001>` | 400 | `<含义>` | `<提示>` |
| `<CODE_002>` | 409 | `<含义>` | `<提示>` |

---

## 5. 测试清单

### 5.1 核心测试点

| TC-ID | 类型 | 场景描述 | 覆盖规则ID | 预期结果 |
|-------|------|----------|------------|----------|
| TC-01 | 集成 | `<场景>` | BR-01 | `<结果>` |
| TC-02 | 单测 | `<场景>` | BR-02 | `<结果>` |

### 5.2 必测边界

- `<并发冲突>`
- `<超时/过期边界值>`
- `<权限越权>`
- `<重复提交幂等>`

### 5.3 质量门禁

- checkstyle：通过
- 全量测试（含前置批次）：通过
- 核心验收用例：通过

---

## 6. 代码组织

> 说明本批次的代码结构和模块划分，以及对已有代码的改造点。

```
specflow-api/src/main/java/com/specflow/api/modules/
├── <module>/
│   ├── interfaces/
│   │   ├── <Module>Controller.java
│   │   └── dto/
│   ├── application/
│   │   └── <Module>Service.java
│   ├── domain/
│   │   ├── entity/
│   │   └── repository/
│   └── infrastructure/
│       └── persistence/
```

### 对已有代码的改造点（如有）

| 文件 | 改造内容 |
|------|----------|
| `<已有文件>` | `<改造描述>` |

### 跨模块依赖（如有）

- `<ModuleA>Service` → `<ModuleB>Repository`（`<依赖原因>`）

---

## 7. 快审记录

| 日期 | 结论（通过/退回） | 关键问题 | Owner | 截止时间 |
|------|--------------------|----------|-------|----------|
| - | - | - | - | - |

---

## 8. 变更联动

> 记录本批次与其他批次之间的联动变更。

| 变更ID | 来源 | 变更摘要 | 对 Tech Pack 影响 | 状态 |
|--------|------|----------|-------------------|------|
| CL-01 | `<方向>` | `<摘要>` | `<影响>` | 待实施 |
