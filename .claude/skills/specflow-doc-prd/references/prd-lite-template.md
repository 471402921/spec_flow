# PRD-Lite 模板（小团队 + AI 80%）

> 模块名称：`<模块名>`
> 版本：`v0.1`
> 状态：`草稿 / 定稿`
> 更新时间：`YYYY-MM-DD`
> 负责人：`产品&UI&UE`
> 基于流程：`doc/README.md`

---

## 1. 概述

本文档定义 `<模块名>` 的功能需求与业务规则，供开发团队完成数据库设计、接口设计和产品开发。

**文档范围**：`<覆盖的功能领域，用顿号分隔>`

**不覆盖**：`<明确排除的功能或技术细节>`

---

## 2. 角色与权限

### 2.1 角色定义

> 描述系统中的角色、获得方式和相互关系。如果一个用户可以同时拥有多种角色，需在此说明。

| 角色 | 获得方式 | 说明 |
|------|---------|------|
| `<角色A>` | `<如何获得>` | `<描述>` |
| `<角色B>` | `<如何获得>` | `<描述>` |

> **说明**：
> - `<角色间的关系补充说明>`

### 2.2 权限矩阵

> 按操作对象维度列出权限，明确"谁可以做什么"。如有多个操作对象，分表列出。

**`<操作对象A>` 相关权限**：

| 操作 | `<角色A>` | `<角色B>` | `<角色C>` |
|------|----------|----------|----------|
| `<操作1>` | ✅ | ✅ | ❌ |
| `<操作2>` | ✅ | ❌ | ❌ |

> **说明**：`<权限规则的补充说明>`

**`<操作对象B>` 相关权限**（如有）：

| 操作 | `<角色A>` | `<角色B>` |
|------|----------|----------|
| `<操作1>` | ✅ | ❌ |
| `<操作2>` | ✅ | ✅ |

---

## 3. 核心实体与关系

### 3.1 实体

| 实体 | 说明 |
|------|------|
| `<实体A>` | `<一句话描述>` |
| `<实体B>` | `<一句话描述>` |

### 3.2 实体关系

```
<实体A> 1 ──── N <实体B>          <关系描述>
<实体A> N ──── M <实体C>          <关系描述>
```

- `<关系规则1：如归属、共享、可选性>`
- `<关系规则2>`

---

## 4. 功能需求

> 按功能领域分组，每个功能点使用 FR 编号。只描述业务行为，不涉及技术实现。

### 4.1 `<功能领域A>`

#### FR-4.1.1 `<功能点名称>`

- `<功能描述>`
- `<业务规则>`
- `<字段要求（必填/选填、格式约束、默认值）>`

#### FR-4.1.2 `<功能点名称>`

- `<功能描述>`

### 4.2 `<功能领域B>`

#### FR-4.2.1 `<功能点名称>`

- `<功能描述>`

### 4.3 `<功能领域C>`（如有）

#### FR-4.3.1 `<功能点名称>`

- `<功能描述>`

---

## 5. 业务规则

### 5.1 关键约束

> 仅保留"必须正确实现"的规则，建议 10~20 条。
> - **优先级**：规则本身的重要性（P0=核心规则，P1=重要规则，P2=增强规则）
> - **批次**：该规则在哪个开发批次实现（可与优先级不同，如核心规则因依赖关系延后实现）

| 编号 | 规则 | 优先级 | 批次 |
|------|------|--------|------|
| BR-01 | `<规则>` | P0 | P0 |
| BR-02 | `<规则>` | P0 | P1 |
| BR-03 | `<规则>` | P1 | P1 |

### 5.2 边界场景

> 列出用户可能遇到的异常情况和预期处理方式。

| 场景 | 处理方式 |
|------|----------|
| `<异常场景A>` | `<拒绝/允许/提示内容>` |
| `<异常场景B>` | `<拒绝/允许/提示内容>` |

---

## 6. 验收标准（用户视角）

> 每条验收标准必须可测试、可追溯到业务规则，按开发批次分组。

### P0 批次验收标准

| AC-ID | 验收描述 | 验收方式 | 覆盖规则 |
|-------|----------|----------|----------|
| AC-01 | `<用户可以...>` | 自动 | BR-xx |
| AC-02 | `<用户不能...>` | 自动 | BR-xx |

### P1 批次验收标准

| AC-ID | 验收描述 | 验收方式 | 覆盖规则 |
|-------|----------|----------|----------|
| AC-xx | `<用户可以...>` | 自动 | BR-xx |

### P2 批次验收标准（如有）

| AC-ID | 验收描述 | 验收方式 | 覆盖规则 |
|-------|----------|----------|----------|
| AC-xx | `<用户可以...>` | 手工 | BR-xx |

---

## 7. 交付约束与分期策略

### 7.1 分期策略

> 说明功能如何拆分为多个开发批次，以及每个批次的交付范围和目标。

| 批次 | 范围 | 功能需求 | 说明 |
|------|------|----------|------|
| **P0 - 核心** | `<功能范围>` | `<FR编号列表>` | `<交付目标>` |
| **P1 - 扩展** | `<功能范围>` | `<FR编号列表>` | `<交付目标>` |
| **P2 - 增强** | `<功能范围>` | `<FR编号列表>` | `<交付目标>` |

> **注意事项**（如有）：`<批次间的依赖关系或特殊说明>`

### 7.2 交付约束（给 Tech Pack）

> 本节用于约束技术文档，避免规则丢失。按批次列出约束。

### P0 批次

- 必须映射的规则：`BR-01, BR-02, ...`
- 必须覆盖的验收项：`AC-01 ~ AC-xx`
- 必须覆盖的边界场景：
  - `<场景A>`
  - `<场景B>`

### P1 批次

- 必须映射的规则：`BR-xx, ...`
- 必须覆盖的验收项：`AC-xx ~ AC-xx`
- 必须覆盖的边界场景：
  - `<场景C>`

### P2 批次（如有）

- 必须映射的规则：`BR-xx, ...`
- 必须覆盖的验收项：`AC-xx ~ AC-xx`
- 必须覆盖的边界场景：
  - `<场景D>`

---

## 8. 未来扩展（本版本不实现）

> 在架构设计中预留扩展点，但不在本版本实现的功能。帮助开发团队理解长期方向，避免短期设计挡路。

- `<未来功能A>：<简要描述>`
- `<未来功能B>：<简要描述>`
